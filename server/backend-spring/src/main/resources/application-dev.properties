# ── Development overrides ──────────────────────────────────────────────────────
# This profile is active when you run locally without setting env vars.
# Copy .env.example to .env — IntelliJ can load it via EnvFile plugin,
# or just set the env vars in your Run Configuration.

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.com.eventzen=DEBUG
